{% extends 'main.html.twig' %}

{% block title %}404 Not Found @ {{ sys.system.name }}{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-lg-12">
            <div class="page-header">
                <h1>404 Not Found</h1>
            </div>
        </div>
    </div>

    <div class="alert alert-danger">
        {{ path }}{% if params %}?{{ params }}{% endif %}
    </div>

{% endblock %}
